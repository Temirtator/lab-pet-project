server:
  port: 8080
  tomcat:
    threads:
      max: 20
      min-spare: 5
    accept-count: 50

spring:
  application:
    name: lab-pet-project

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

app:
  security:
    username: admin
    password: admin
